{
    programs.fuzzel = {
        enable = false;

        settings = {
            main = {
                width = 40;
                vertical-pad = 10;
                inner-pad = 10;
                lines = 8;
                line-height = 30;
            };
        };
    };

    stylix.targets.fuzzel.enable = false;
}