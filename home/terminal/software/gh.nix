{
    programs.gh = {
        enable = true;

        settings = {
            # keep-sorted start
            editor = "code --wait";
            git_protocol = "https";
            # keep-sorted end
        };
    };
}
